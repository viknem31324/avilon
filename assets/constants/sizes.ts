export const DEFAULT_SIZES = ['s', 'm', 'l'] as const;

export const DEFAULT_SIZES_WITH_EMPTY = ['', 's', 'm', 'l'] as const;

export const DEFAULT_SIZES_WITH_EXTRA_SMALL = ['xs', 's', 'm', 'l'] as const;
